{
  "name": "dapp-learning-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dapp-learning-test",
    "create-local": "graph create --node http://localhost:8020/ dapp-learning-test",
    "remove-local": "graph remove --node http://localhost:8020/ dapp-learning-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dapp-learning-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
